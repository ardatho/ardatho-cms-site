import roles from './roles';
import users from './users';

import pages from './pages';

export default {
  contentLang: ['fr', 'en'],
  contentLangDefault: 'en',
  types: [
    users,
    roles,
    pages,
  ]
}